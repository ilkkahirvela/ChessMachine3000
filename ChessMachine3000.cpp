#include "chess.h"
#include "position.h"
#include <iostream>

using namespace std;

int main()
{
    Position position;

    position.printBoard();

    //// Kuninkaan sijainti
    //int row, column;

    //position.findKing(wK, row, column);

    //if (row != -1 && column != -1) {
    //    cout << "Valkoinen kuningas sijainnissa: (" << row << ", " << column << ")";
    //}

    return 0;
}
